import { GlucoseImportScreen } from '@/features/glucose/components/GlucoseImportScreen/GlucoseImportScreen';

import type { ReactElement } from 'react';

const GlucoseImport = (): ReactElement => <GlucoseImportScreen />;

export default GlucoseImport;
